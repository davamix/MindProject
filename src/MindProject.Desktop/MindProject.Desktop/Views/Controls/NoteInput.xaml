<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MindProject.Desktop.Views.Controls.NoteInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MindProject.Desktop.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="noteInputControl">

    <Grid Background="#AA000000">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBox x:Name="txtNewNote"
				 Grid.Row="0"
				 VerticalAlignment="Stretch"
				 HorizontalAlignment="Stretch"
				 Margin="68"
				 Padding="10"
				 TextWrapping="Wrap"
				 AcceptsReturn="True"
				 PlaceholderText="Enter your note here..."
				 Text="{Binding ElementName=noteInputControl, Path=NoteContent, Mode=TwoWay}"/>

		<Button x:Name="btnSaveNote"
				Grid.Row="1"
				Content="Save"
				Click="btnSaveNote_Click" />

	</Grid>
</UserControl>
