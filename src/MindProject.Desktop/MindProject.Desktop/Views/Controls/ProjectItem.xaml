<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="MindProject.Desktop.Views.Controls.ProjectItem"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="using:MindProject.Desktop.Views.Controls"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
			 x:Name="projectItemControl">

	<Grid RowSpacing="5"
		  Margin="16" 
		  Background="Transparent">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition />
			<RowDefinition />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0"
				   Grid.Column="0"
				   Grid.ColumnSpan="2"
				   Text="{Binding ElementName=projectItemControl, Path=Project.Name}"
				   Style="{ThemeResource BodyStrongTextBlockStyle}" />

		<TextBlock Grid.Row="1"
				   Grid.Column="0"
				   Grid.ColumnSpan="2"
				   Text="{Binding ElementName=projectItemControl, Path=Project.Description}"
				   Style="{ThemeResource BodyTextBlockStyle}"
				   TextWrapping="Wrap" />

		<TextBlock Grid.Row="2"
				   Grid.Column="0"
				   Text="{Binding ElementName=projectItemControl, Path=UpdatedAtString}"
				   Style="{ThemeResource CaptionTextBlockStyle}"
				   VerticalAlignment="Center" />

		<HyperlinkButton Grid.Row="2"
						 Grid.Column="1"
						 NavigateUri="{Binding ElementName=projectItemControl, Path=Project.RepoAddress}"
						 HorizontalAlignment="Right"
						 ToolTipService.ToolTip="{Binding ElementName=projectItemControl, Path=Project.RepoAddress}">
			<FontIcon Glyph="&#xE71B;"
					  Foreground="Gray"/>
		</HyperlinkButton>
	</Grid>
</UserControl>
